filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /logs/*.log
    fields:
      log_type: express-app
    fields_under_root: true
    json.keys_under_root: true
    json.add_error_key: true
    json.message_key: message

filebeat.modules:
  - module: system
    syslog:
      enabled: true
    auth:
      enabled: true

output.elasticsearch:
  hosts:
    ["https://993932b7dc704256a38e27292d46ffb3.us-central1.gcp.cloud.es.io:443"]
  username: "elastic"
  password: "S9WjLRWpwnOpqoyJis1LCzIq"
  indices:
    - index: "filebeat-%{[agent.version]}-%{+yyyy.MM.dd}"

setup.kibana:
  host: "http://kib01:5601"

logging.level: info
